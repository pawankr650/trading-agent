# core (all systems)
pandas>=2.2
numpy>=1.26
requests>=2.31
pyyaml>=6.0
python-dotenv>=1.0
matplotlib>=3.8
yfinance>=0.2.60        # older versions fail with "Invalid Crumb" (HTTP 401) from Yahoo
scipy>=1.11             # swing-pivot detection for chart patterns

# web app backend (python -m server.app)
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27             # used by the API tests (fastapi.testclient)
TA-Lib>=0.6             # 61 candlestick recognisers (0.6+ ships binary wheels, no C build)
backtesting>=0.6        # kernc/backtesting.py — Algo Lab simulations + optimiser
huggingface_hub>=0.24   # HF Inference API for FinBERT sentiment + zero-shot event typing

# run the Hugging Face news models locally instead of the API (heavier, optional):
#   pip install transformers torch --extra-index-url https://download.pytorch.org/whl/cpu

# live data / live trading through 30+ Indian brokers (optional)
openalgo>=2.0

# optional deep multi-agent analysis (slow, heavy):
#   git clone https://github.com/TauricResearch/TradingAgents && pip install ./TradingAgents
